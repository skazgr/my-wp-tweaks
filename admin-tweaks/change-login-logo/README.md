# Change Login Logo

Replaces the default WordPress login page logo with a custom image from your Media Library.

## Usage

1. Upload your logo image to the Media Library.
2. Replace the URL in `change-login-logo.php` with your image’s full URL.
3. Copy the PHP code into your theme’s `functions.php` or a custom plugin.

## Notes

- Adjust your logo size.
